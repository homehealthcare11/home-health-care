<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uptech.android_uikit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UptechUIKit">
        <activity android:name=".ecomerceapp.login.RegisterActivity"/>
        <activity android:name=".ecomerceapp.login.LogInActivity" />
        <activity android:name=".ecomerceapp.activities.ProductDetail" />
        <activity android:name=".ecomerceapp.activities.CheckoutActivity" />
        <activity android:name=".ecomerceapp.activities.EcomerceMainActivity" />
        <activity android:name=".ecomerceapp.activities.CartActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".upBlog.singlePost.SinglePostFullImgActivity" />
        <activity android:name=".upBlog.singlePost.SinglePostCommentsActivity" />
        <activity android:name=".upBlog.singlePost.SinglePostActivity" />
        <activity android:name=".upBlog.loginRegistration.RegistrationActivity" />
        <activity android:name=".upBlog.loginRegistration.LoginActivity" />
        <activity android:name=".upBlog.UpBlogMainActivity">
        </activity>
    </application>

</manifest>